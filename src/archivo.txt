MAIN {
    ulong CONTADOR;
    CONTADOR := - 2UL;
    X,Y = 1UL, 2UL;
    while (CONTADOR ++++ 1UL) do {
        CONTADOR := 4UL;
    };
    print(CONTADOR);
    X := X + 1UL;
    print(X);
    print(Y);
    ulong F (ulong X, ulong M) {
        
    }
}
