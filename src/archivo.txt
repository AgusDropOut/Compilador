MAIN {
    ulong CONTADOR;
    CONTADOR := CONTADOR + 5.4D-4;
    X,Y = 1UL, 2UL;
    while (CONTADOR < 1UL) do {
        CONTADOR := CONTADOR + 1UL;
    };
    print(CONTADOR);
    X := X + 1UL;
    print(X);
    print(Y);
    ulong F (ulong X, ulong M) {
        
    }
}
