MAIN {
    ulong CONTADOR;
    CONTADOR := - 2UL;
    X , Y = 1UL , 2UL;
    if (X < Y) {
        print(Y);
    } else {
        print(Y);
    } endif ;
    CONTADOR := + 5UL;
    print(CONTADOR);
    X := X + 1UL;
    print(X);
    print(Y);
    ulong F (cr ulong X, ulong M) {

    }
}
