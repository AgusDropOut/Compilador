MAIN {
    ulong CONTADOR;
    CONTADOR := -5.4D-4 + 4UL;
    X,Y = 1UL, 5UL;
    if (3UL <  1UL)  {
        CONTADOR := 4UL;
    } endif ;
    print(CONTADOR);
    X := X + 24UL;
    print(X);
    print(Y);

    ulong F (ulong X, ulong M) {
       ulong CONTADOR;
       ulong A (ulong X, ulong M) {
            ulong CONTADOR;
            CONTADOR := 5UL;
       }
       ulong B (ulong X, ulong M) {
            ulong C (ulong X, ulong M) {

            }
       }
    }
}