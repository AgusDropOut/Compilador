PROGRAMAPAR {
    ## Devuelve 1 si es Par, 0 si es Impar ##


    ulong ESPAR(ulong NUM, cr ulong ESPAR2) {
        ulong MITAD;
        ulong RECONSTRUIDO;

        ulong H (ulong X){
           print(X);
           return(0UL);
        }

        MITAD := NUM / 2UL;
        RECONSTRUIDO := MITAD * 2UL + H(45UL -> X); ##Llamada a funcion H, llamada valida, imprime 45##

        RECONSTRUIDO := PROGRAMAPAR.ESPAR(67UL -> NUM, MITAD -> ESPAR2); ##Llamada a funcion ESPAR, llamada invalida, da error##

        if (RECONSTRUIDO == NUM) {
            ESPAR2 := 1UL;
        } else {
            ESPAR2 := 0UL;
        } endif;

        return (0UL);
    }

    ulong X;
    ulong CHECK;
    ulong DUMMY;
    X := 10UL;
    CHECK := 0UL;

    DUMMY := ESPAR(X->NUM, CHECK->ESPAR2);

    ## Imprime 1 si es par, 0 si impar ##
    if(CHECK == 1UL) {
        print("Es par");
    } else {
        print("Es impar");
    } endif;

}