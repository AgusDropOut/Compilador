TESTRECURSIVO {
    ulong GLOBALA;
    ulong GLOBALB;
    ulong X , Y , Z;



    print(X);

    GLOBALA := 10UL;
    GLOBALB := 7UL;

      ulong G (ulong CONT){
            ulong LOCALG;
            ulong TEMPVALOR;

            LOCALG := TESTRECURSIVO.GLOBALB;
            (ulong A){A:= A + 1UL;print(A);}(34UL);

            if (LOCALG < 45UL){
              print("Hola");
            }
            else{
                TEMPVALOR := CONT;
            }
            endif;

            LOCALG := LOCALG + CONT;

            return (4UL);
     }

    ulong F (ulong VALOR){
        ulong LOCALF;

        if (VALOR < TESTRECURSIVO.GLOBALB) {
            print("F: Caso Base");
            return (VALOR);
        }
        endif;

        LOCALF := TESTRECURSIVO.G(VALOR -> CONT);

        LOCALF := LOCALF + VALOR;

        return (LOCALF);
    }

    ulong RESULTADO;
    RESULTADO := TESTRECURSIVO.F(GLOBALA -> VALOR);

    print("Resultado de la recursividad:");
    print(RESULTADO);
    print("Fin del test");
}